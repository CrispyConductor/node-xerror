{
  "name": "xerror",
  "version": "2.0.0",
  "description": "Error wrapper including things like error code, data, and cause for Node.JS",
  "main": "./xerror.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:crispy1989/node-xerror.git"
  },
  "scripts": {
    "test": "node ./node_modules/mocha/bin/mocha ./test -R spec",
    "lint": "node ./node_modules/jshint/bin/jshint --reporter unix --exclude ./node_modules . || exit 0",
    "watch": "node ./node_modules/mocha/bin/mocha --watch ./test -R spec"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^10.1.0"
  },
  "license": "Apache-2.0"
}
